import React from "react";
import './Language.css'

const Language = () => {
  return (
    <select className="form-select action-language">
      <option selected>English</option>
    </select>
  );
};

export default Language;
